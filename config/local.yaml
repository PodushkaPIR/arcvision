env: local

http_server:
  address: ":8082"
  timeout: 5s
  idle_timeout: 60s

ai:
  provider: 
  base_url:
  model:

deck:
  deck_path: "./web/data/deck.json"
  spread_path: "./web/data/spreads.json"
